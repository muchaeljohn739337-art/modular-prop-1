{
  "name": "advanciapayledger-deployment",
  "version": "1.0.0",
  "description": "Deployment verification for Advancia PayLedger",
  "scripts": {
    "verify:build": "cd frontend && npm run build",
    "verify:backend": "cd backend && npm run build",
    "verify:all": "npm run verify:backend && npm run verify:build",
    "deploy:check": "node scripts/deployment-check.js"
  }
}
