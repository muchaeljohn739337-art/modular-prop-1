# ============================================
# IN-MEMORY STORAGE - NO DATABASE REQUIRED
# ============================================
# This backend uses in-memory storage (JavaScript Maps)
# Data is stored in RAM and cleared on server restart
# No MongoDB, PostgreSQL, Redis, or other database needed!
#
# For production with persistent data, see README.md

# Server Configuration  
NODE_ENV=development
PORT=4000

# JWT Secret (change in production!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Settings
# Development: Allow all origins
CORS_ORIGIN=*
# Production: Set to your frontend domain
# CORS_ORIGIN=https://www.advanciapayledger.com

# Rate Limiting (per IP address)
RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT=100

# ============================================
# OPTIONAL: For future database integration
# ============================================
# Uncomment and configure if upgrading to MongoDB:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/advanciapayledger
# MONGODB_DB_NAME=advanciapayledger

# Uncomment if adding Stripe payments:
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Uncomment if adding email notifications:
# RESEND_API_KEY=re_your_resend_api_key
# FROM_EMAIL=noreply@advanciapayledger.com

